# dependencies
node_modules/

# build output
out/

# packaged extension
*.vsix

# bundled portable node runtime (shipped in the vsix, not committed)
vendor/

# logs
*.log

# 排除所有以 . 开头的文件夹（.vscode、.idea 等）
.*/

# stray known_hosts written by ssh with a mangled path
D:*known_hosts*
